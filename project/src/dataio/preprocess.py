"""Placeholder preprocessing script for fMRI data."""
from __future__ import annotations


def preprocess_dataset(config: dict) -> None:
    """Pretend to preprocess an fMRI dataset.

    The implementation is intentionally left as a placeholder so that the rest
    of the repository can focus on model architecture. Future work can replace
    this function with real data handling.
    """

    _ = config
